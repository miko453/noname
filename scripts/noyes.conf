# Intentionally weak client-side SSH options for legacy hosts.
# Keep this profile only for trusted/private environments.
KexAlgorithms +diffie-hellman-group1-sha1
IdentitiesOnly yes
ConnectTimeout 10

Host *
   StrictHostKeyChecking no
   UserKnownHostsFile=/dev/null
